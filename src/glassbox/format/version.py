"""ContextPack format version.

Follows semver. Breaking changes to the envelope fields bump the major.
New section types or optional fields bump the minor.
Bug fixes to validation logic bump the patch.
"""

FORMAT_VERSION = "0.1.0"
